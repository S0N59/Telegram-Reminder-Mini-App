# 🔔 Telegram Reminders Mini App

Современное Telegram Mini App для создания и управления напоминаниями с автоматической отправкой уведомлений в Telegram.

## ✨ Особенности

- 🎨 Современный, минималистичный дизайн
- 📱 Полная оптимизация для мобильных устройств
- 🔔 Автоматическая отправка уведомлений в Telegram
- ⏰ Планировщик напоминаний с проверкой каждую минуту
- 🎯 Интеграция с Telegram WebApp SDK
- 💾 Локальное хранение напоминаний
- 🌈 Поддержка тем Telegram (светлая/тёмная)

## 🛠 Технологии

- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик
- **Telegram WebApp SDK** - Интеграция с Telegram
- **Telegram Bot API** - Отправка уведомлений

## 📦 Установка

```bash
npm install
```

## 🚀 Запуск

```bash
npm run dev
```

## 🔧 Настройка Bot Token

Для работы уведомлений необходимо настроить Telegram Bot:

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите Bot Token
3. Создайте файл `.env` в корне проекта:

```env
VITE_BOT_TOKEN=your_bot_token_here
```

**⚠️ ВАЖНО:** В продакшене Bot Token должен храниться на backend для безопасности!**

### Альтернатива: Использование Backend

Вместо прямого вызова Bot API можно использовать backend endpoint:

```env
VITE_BACKEND_URL=https://your-backend.com/api
VITE_USE_BACKEND=true
```

Backend должен принимать POST запросы на `/send-notification`:

```json
{
  "chatId": 123456789,
  "text": "Текст напоминания"
}
```

## 📁 Структура проекта

```
src/
  components/           # React компоненты
    ReminderForm.tsx    # Форма создания напоминания
    ReminderList.tsx    # Список напоминаний
  types/               # TypeScript типы
    telegram.d.ts       # Типы Telegram WebApp SDK
    reminder.ts         # Типы напоминаний
  utils/               # Утилиты
    telegram.ts         # Работа с Telegram WebApp
    reminder.ts         # Работа с напоминаниями
    bot.ts              # Отправка уведомлений через Bot API
    reminderScheduler.ts # Планировщик напоминаний
  config.ts            # Конфигурация приложения
  App.tsx              # Главный компонент
  main.tsx             # Точка входа
```

## 🎨 Дизайн

- **Mobile-first** подход
- Адаптивная верстка для всех размеров экранов
- Плавные анимации и переходы
- Поддержка системной темы Telegram
- Touch-friendly интерфейс

## 📡 API для Backend

При создании напоминания формируется payload:

```typescript
{
  text: string;        // Текст напоминания
  time: string;         // Время в формате HH:mm
  userId: number;       // ID пользователя Telegram
  initData: string;     // Telegram initData для валидации
}
```

## 🔔 Как работают уведомления

1. Пользователь создаёт напоминание с текстом, датой и временем
2. Когда мини-приложение открыто, планировщик проверяет напоминания каждую минуту
3. Когда наступает время напоминания, отправляется сообщение в Telegram
4. **Важно:** Уведомления работают только когда мини-приложение открыто

## 🏗 Сборка

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## 📝 Лицензия

MIT
