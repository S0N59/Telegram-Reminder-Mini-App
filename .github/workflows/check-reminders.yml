name: Check Reminders

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  check-reminders:
    runs-on: ubuntu-latest
    steps:
      - name: Send Due Reminders
        run: |
          curl -s -H "x-api-key: ${{ secrets.SCHEDULER_API_KEY }}" \
            "https://backend-9asrt4ke5-norayrs-projects-a813a9d8.vercel.app/api/check-reminders"
